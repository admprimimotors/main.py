"""
Paquete ml — todo lo relacionado con la API de Mercado Libre.

Módulos:
    auth         — OAuth 2.0 + gestión de tokens (access + refresh)
    client       — (futuro) wrapper HTTP con retry y rate limit
    stock_sync   — (futuro) sincronización de inventario
    publicaciones— (futuro) publicación masiva
    precios      — (futuro) actualización masiva de precios
    respuestas   — (futuro) respuestas automáticas a preguntas
"""
